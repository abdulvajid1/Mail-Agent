[project]
name = "job-mail-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "grandalf>=0.8",
    "ipykernel>=7.3.0",
    "langchain>=1.3.11",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.7",
]

[dependency-groups]
dev = [
    "ruff>=0.15.18",
]


[project.scripts]
job-agent = "job_agent.agent.agent:main"

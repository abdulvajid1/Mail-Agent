[project]
name = "job-mail-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "fastmcp>=3.4.4",
    "grandalf>=0.8",
    "ipykernel>=7.3.0",
    "langchain>=1.3.11",
    "langchain-mcp-adapters>=0.3.0",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.7",
    "mcp>=1.28.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.18",
]


[project.scripts]
job-agent = "job_agent.cli.main:main_cli"

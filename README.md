# Job-Mail-Agent


## Setup
uv sync
uv pip install -e .

## start project
fastmcp run "src/job_agent/mcp/server.py"


mail-agent setup -> ollama, mail? auth, llm
mail-agent start -> agent start 
mail-agent auth -> mail authentication
mail-agent enable-mail --false, --True -> enable tool in agent